# ── Numa Requirements ─────────────────────────────────────────────────────────
# DO NOT run with bare 'pip install' - always use setup.bat
# which installs into the virtual environment correctly.
#
# Manual install: .venv\Scripts\pip.exe install -r requirements.txt

# ── AI / Speech ───────────────────────────────────────────────────────────────
google-genai
openai-whisper
python-dotenv

# ── Wake word detection ───────────────────────────────────────────────────────
openwakeword
onnxruntime

# ── Audio input/output ────────────────────────────────────────────────────────
sounddevice
numpy
pygame
edge-tts
pyttsx3
pyaudio

# ── System control ────────────────────────────────────────────────────────────
pyautogui
psutil
pyperclip
requests
scipy

# ── Volume control (Windows only) ────────────────────────────────────────────
pycaw; sys_platform == "win32"
comtypes; sys_platform == "win32"
pywin32; sys_platform == "win32"

# ── UI ────────────────────────────────────────────────────────────────────────
PyQt6